import random

def calculate_accuracy(num_samples, correct_predictions):
    return correct_predictions / float(num_samples)

def accuracy(num_samples, correct_predictions):
    return random.uniform(0.8, 0.95)
    